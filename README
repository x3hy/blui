Basic 3d rendering engine for the terminal.
---

The "shapes" folder contains configurations for a few different shapes, include a shape in render.c, compile with the following:
gcc render.c -o blui && ./blui
