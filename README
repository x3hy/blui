Basic 3d rendering engine for the terminal.
---

The shapes directory contains some separate shapes which can be rendered with render.c. To render a shape simply run the ./demo file and use the arrow keys to select a shape to draw.
